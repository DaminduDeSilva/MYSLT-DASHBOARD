name: Simple Deploy

on:
  push:
    branches: [main]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: |
          echo "🚀 Hello from GitHub Actions!"
          echo "We're testing CI/CD for MySLT Dashboard"
          echo "This is running on commit: ${{ github.sha }}"